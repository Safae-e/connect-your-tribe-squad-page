{% render 'partials/head.liquid', href: 'style.css' %}

<h1> {{ team_name }} </h1>

<ul>
  {% for person in persons %}
    <li><a href="/detail/{{ person.id }}">{{ person.name }} {{ person.team }} </a></li>
  {% endfor %}
</ul>

{% render 'partials/foot.liquid', squads: squads %}
